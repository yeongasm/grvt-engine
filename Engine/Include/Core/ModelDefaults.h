#pragma once

#ifndef GRAVITY_CORE_MODELDEFAULTS
#define GRAVITY_CORE_MODELDEFAULTS

#include "Framework/Abstraction/Model.h"

namespace Grvt
{

	//ModelCreationInfo GenerateDefaultCircle(uint32 EdgeSegments, uint32 RingSegments);

	//ENGINE_API ModelCreationInfo GenerateDefaultSphere(uint32 HorizontalSegments = 32, uint32 VerticalSegments = 32);

	//ModelCreationInfo GenerateDefaultPlane(uint32 HorizontalSegmnets, uint32 VerticalSegments);

	//ModelCreationInfo GenerateDefaultQuad(float32 Width, float32 Height);

	//ModelCreationInfo GenerateDefaultCube();

	//ModelCreationInfo GenerateDefaultTorus(float32 R1, float32 R2, uint32 Lod1, uint32 Lod2);
}

#endif // !GRAVITY_CORE_MODELDEFAULTS